# def format_grades(grades):
#     students = list(grades)
#     scores = list(grades[x] for x in students)
#     avg = list(sum(scores[y] / len(scores[y]) for y in scores))

#     return '/n'.join(a + b for a in students and b in avg)